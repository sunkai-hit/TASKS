# Execution Logs

这里只保存必要的运行摘要，不保存模型内部推理过程。

建议文件名：

`YYYY-MM-DD.md`

每条日志记录：
- run_id
- task_id
- started_at / finished_at
- 完成的可验证动作
- 生成的 commit / file / PR
- 状态迁移
- 错误或阻塞

任务的权威状态仍以 `queue/tasks.json` 为准。